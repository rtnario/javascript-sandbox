/*
    NODES 101!
        - in HTML, they're called tags/elements
        - in DOM terminilogy, they're called nodes

    ROOT NODE:          top node in tree
    CHILD NODE:         node directly inside another node
    DESCENDANT NODE:    node anywhere inside another node (child of a child, etc)
    PARENT NODE:        node with another node inside
    SIBLING NODE:       node on the same innter-outer level as another node
*/

